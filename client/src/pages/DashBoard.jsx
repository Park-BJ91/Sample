import React, { useState } from 'react';

export default function DashBoard() {
    const [data, setData] = useState(null);

    return (
        <div>
            <h1>Dashboard</h1>
            {/* 대시보드 내용 */}
        </div>
    );
}
